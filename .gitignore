# Godot engine files
.godot/
*.translation

# Export presets (contain absolute paths, platform-specific)
export_presets.cfg

# macOS
.DS_Store

# Windows
Thumbs.db

# Editor temp files
*.tmp

# Export output
/build/

# Epic Online Services secrets
eos_credentials.cfg

# Android gradle build output (template itself is tracked — it carries EOS patches)
android/build/build/
android/build/.gradle/
android/build/libs/debug/
android/build/libs/release/
android/build/assets/
android/build/assetPackInstallTime/src/main/assets/
android/build/src/main/assets/
